namespace GarageManagement.Application.Enums
{
    /// <summary>
    /// Type of lookup data to return. Add values as new lookup sources are added.
    /// </summary>
    public enum LookupType
    {
        GarageService = 1
    }
}
